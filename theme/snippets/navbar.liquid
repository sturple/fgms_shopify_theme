
{%assign navLinkList = linklist %}
{%assign navType = type %}		
<nav class="navbar   {{navType}}" role="navigation">
	{% if navType == blank or navType == 'main' %}
	<div class="navbar-header">			
		<a type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
			<div class="pull-left">MENU</div>
			<div class="pull-right" style="margin-top: 2px;">
				<span class="sr-only">TOGGLE MENU</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>					
			</div>
			<div class="clearfix"></div>
		</a>
	</div>
	{% endif %}
	{% if navType == blank or navType == 'main' %}<div class="navbar-collapse collapse ">{% endif %}			
		<ul class="nav navbar-nav">
			{% for link in linklists[navLinkList].links %}							
			{% capture child_list_handle %}{{ link.title | handleize }}{% endcapture %}               
				{% if linklists[child_list_handle] and linklists[child_list_handle].links.size > 0 and navType == 'main' %}
					<li class="dropdown ">
						<a href="{{ link.url }}" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">{{ link.title }} <span class="caret"></span></a>
						<ul class="dropdown-menu" role="menu">                                                            
							{% for l in linklists[child_list_handle].links %}
								<li{% if l.active %} class="active"{% endif %}><a class="subnav" href="{{ l.url }}">{{ l.title }}</a></li>
							{% endfor %}
						</ul>
					</li>
				{% else %}
					<li {% if link.active %} class="active"{% endif %} >
						<a {% if link.type == 'http_link' %}target="_blank"{% endif %} class="{{link.type}}" data-menu-scroll href="{{ link.url }}">{{ link.title }}</a>
					</li>
				{% endif %}
			{% endfor %}
		</ul>
	{% if navType == blank or navType == 'main' %}</div>{% endif %}
</nav> 